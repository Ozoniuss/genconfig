# note that path is required if using this approach, because without generate
# tags genconfg doesn't know where you placed the struct!
./genconfig -struct=MyConfig -path=config/myconfig.go -project=Myapp -debug