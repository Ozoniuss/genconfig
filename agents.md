# Agent Instructions

Before committing any changes, read the `justfile` to understand what needs to be done. In particular, run `just update-all` before every commit to ensure all tests pass and all examples are regenerated with the latest code.
